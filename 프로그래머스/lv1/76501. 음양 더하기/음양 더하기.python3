def solution(absolutes, signs):
    length = len(absolutes)
    ans = 0
    for i in range(length):
        if signs[i]:
            ans += absolutes[i]
        else:
            ans -= absolutes[i]
    return ans